# 验证记录

日期：2026-09-07。基于上游 esp-launchpad 提交 `30eca54e65dd530dbf46a809857ea8b3915f4748`。

## 自动化

- `npm test`：15 项通过（恢复、失败后的重试、取消、后台页面、计时器延迟、关闭失败、镜像边界、C6 基址）。
- `npm run test:browser`：2 项通过，使用已安装的桌面 Chrome 的独立无头测试配置。
- 真实依赖加载无页面异常、无外部 CDN 请求；MD5 的 abc 测试向量正确。
- 模拟串口验证完整 UI 连接、Flash ID、显式覆盖确认及 MD5 回调路径；没有使用实机执行写入。
- 桌面和 390 px 窄屏布局已检查，无横向溢出。

## 实机只读

用户授权 localhost 的 USB Serial/JTAG 后，通过增强页原有连接逻辑测试：

- 芯片：ESP32-C6 revision 2。
- 自动 USB 下载复位三次等待实测为 102、100、101 ms。
- RAM stub 正常加载运行。
- Flash ID：`0x174020`，容量 8 MB。C6 SPI1 基址修正后不再返回 0。
- 从地址 0 读取 4096 字节成功，全部为 FF。
- 读取结束仍保持已连接/下载状态。
- 未执行实机写入、擦除或 eFuse 修改。完整烧录和 MD5 设备端校验仍需使用用户提供的适配固件验证。

脱敏日志仅保存在本地 `docs/hardware-readonly.json`（已从 Git 和发布包排除）。该次实测设备为 8 MB，不能仅根据之前的 N4 名称假定为 4 MB。

## 诊断结论的边界

此前原版网页库实验：后台 100 ms 定时器实际约 1 秒，复位未完成先掉线；前台相同库约 101 ms 并连接成功。
这是一个已复现的失败机制，不等于所有历史失败都来自后台节流。
AppleScript 注入代码的执行环境不应直接等同于页面自身的全局环境；因此仅在注入上下文看到 Terminal 未定义，不足以证明网页本身的依赖加载失败。本项目另用 Playwright 主页面上下文验证依赖加载。
浏览器重枚举可能产生新的 SerialPort 对象。已复现旧对象打开失败，因此修复为每次重试重新获取已授权端口；唯一匹配候选可以替换旧对象，已有会话 MAC 时在加载 stub 前核对，多个候选需用户选择。刷新页面后须重新建立芯片身份记录。

## 发布前复验

- `npm ci` 成功，安装审计 0 个已知漏洞（仅代表当次依赖审计结果）。
- 15 项逻辑测试、2 项 Chrome 测试通过。
- 发布产物在 `/esp-launchpad-enhanced/` 子目录运行，两项 Chrome 测试再次通过。
- 发布包固定白名单；Flash 备份、诊断日志、.git 均不可通过静态服务获取。
- 实机烧录仍未执行；不能据此声称所有芯片或固件组合均已验证。

## 追加恢复验证

- 已复现选择弹窗中设备掉线导致 NotFoundError；新增直接等待已授权设备的连接入口。
- 已复现旧 SerialPort 对象持续打开失败；新版本实测日志出现 port.reenumerated，随后 USB 复位、加载 stub、读取 Flash ID 成功。
- 已连接过的会话再次连接先使用 no_reset，失败回退 usb_reset。
- 自动测试新增已授权设备发现、歧义拒绝、取消、新对象恢复、复位回退和身份不匹配停止。
- 三轮断开重连并读取启动区成功。另发现连续读取偶发数据截短，期间无 USB disconnect；因此不能将该错误等同于芯片重启。
- 对齐 Python 的读取窗口（64）、接收并校验 MD5 尾帧，补齐 SPI attach 及已有 stub 的状态标志。
- 显式设置 Web Serial bufferSize=65536 后，连续五次 4096 字节读取及 MD5 校验通过。修复前曾收到 3786 字节的截短包；该对照支持接收缓冲不足，但未逐一隔离所有协议改动的贡献。
- 以上属于当前模组、本机 Chrome 的有限只读验证，不代表长期稳定性或实机烧录已验证。

- 最终版本保持连接 30 秒后再次读取 4096 字节并通过 MD5 校验，期间网页记录的 USB disconnect 为 0；测试结束仍已连接。
- 最终 15 项逻辑测试、2 项 Chrome 测试和部署子路径下的同两项测试均通过。尚未推送或部署。

## 独立环境启动器（2026-09-08）

- 全新缓存自动检测 macOS ARM64，下载并校验 uv，自动安装独立 Python 3.12.12 与哈希锁定的 esptool 5.4.0，doctor 检查成功。首次测试发现并修复 uv managed-python 与环境变量冲突。
- 使用 UV_OFFLINE=1 再次启动成功，无需再次安装环境。
- 恢复身份、多个设备选择、无设备超时、子进程超时恢复及静态服务限制与重复启动拒绝共 6 项测试通过；浏览器新增助手接管测试。
- 客户 ZIP 在包含空格的路径解压运行成功；包含 19 个白名单文件，保留 Unix 启动入口执行权限，不含芯片备份、设备日志、测试或仓库内容。
- 实机运行：no-reset 初次握手失败后自动回退 usb-reset，ESP32-C6FH8 rev0.2 / 8MB 识别成功，RAM stub 运行，no-reset 退出后释放串口；配套本地网页服务启动成功。没有写入/擦除 Flash 或修改 eFuse。
- Windows、Linux 和其他架构入口已实现，新增跨系统 CI 工作流但尚未运行；不能据此声称这些系统已通过实机验证。

- 助手接管的 no-reset 模式首次授权列表已限定为 ESP 原生 USB VID/PID，并加入浏览器回归，避免误选 Bluetooth/debug 等串口。
- 配套 Python 服务器和部署子路径下各 3 项浏览器测试通过；开发和部署下载地址均返回 200。
- 新站点 localhost:4175 的浏览器端实机接管仍待用户首次授权；当前只验证到 Python 成功稳定 USB 并释放端口，不能将模拟 UI 测试表述为首次授权实机接管成功。

## 自动接管旧助手（2026-09-08）

- 新启动器通过本机私有实例凭据识别旧助手，已就绪时请求其正常退出，等待端口释放再启动；无凭据/其他程序不结束进程，USB 恢复中拒绝替换。
- 8 项助手测试通过：包括真实两个 Python 进程连续启动，第一实例正常退出、第二实例提供网页、退出后清理实例记录；以及无凭据、错误凭据、带 Origin 请求与忙碌状态的拒绝验证。
- 旧版无控制协议的助手仍需首次手动退出；后续新版之间自动接管。此次未操作 USB、未推送或部署。Windows 接管逻辑尚未实机验证。

## 助手网页重连修复（2026-09-08）

- 实际日志复现：USB 约每 2.56 秒断开/上线，四轮连接却全部使用 no_reset。URL 中的模式只代表助手最初的交接状态，不能假设后续设备一直处于下载模式。
- 助手入口改为显式“优先接管，失败自动恢复”模式：先 no_reset，失败后 usb_reset；严格 no_reset 仍作为独立手动选项保留。日志逐轮记录实际使用的模式。
- 15 项逻辑测试、4 项浏览器测试通过，包括首次无 MAC 记录时 no_reset 失败回退 USB 复位，以及非助手入口的严格不复位选项。

- 4175 配套服务器上的 4 项浏览器测试通过。重启本地助手恢复 USB 后，实际网页通过已授权端口以 no_reset 成功接管并读出 Flash ID 0x174020 / 8MB，完成此前待验证的本地助手到网页接管；本轮未实测复位后的网页自动回退，该分支由回归测试覆盖。

## 按授权状态自动连接（2026-09-08）

- 环境准备仍由启动脚本检查独立缓存；浏览器授权由助手网页 getPorts 检查，两个阶段互不混淆。
- 助手入口存在唯一已授权原生 USB 时自动连接；无可用授权或多个候选时提示手动选择，不自动调用 requestPort。首次加载仅自动尝试一次，手动断开后不会自动抢回端口。
- 6 项浏览器测试在开发服务和实际 4175 配套服务通过，包括自动连接失败回退、无授权及多候选保护。
- 本次实机页面自动连接并恢复后读出 C6 / 8MB；随后出现 serial.disconnect，不能把本次自动连接成功当作持续稳定性验证。

## 网页接管确认与助手自动退出（2026-09-08）

- 19 项 JavaScript 单元测试、9 项 Python 测试、6 项浏览器回归通过。
- 新增 2 项完整流程测试通过：真实 Python 服务配合模拟芯片，验证接管后进程退出、端口释放、继续读取及离线刷新后再读取；连接失败时保留助手。
- 实机 C6：Python 自动回退 usb-reset 后恢复下载模式；网页使用已有授权自动以 no_reset 接管，确认 Flash ID 和 MAC 后收到 handoff-accepted。真实助手退出码 0，4175 无监听。
- 助手退出约 100 秒后，实机网页读取启动区 4096 字节成功并通过读取流程 MD5 校验，全部为 FF。实机离线刷新未验证；该路径由完整流程测试覆盖。未执行实机烧录、擦除或 eFuse 操作。
- 更新本地 ZIP 和用户已解压文件夹。Windows/Linux 自动退出流程尚未实机验证；未推送或部署。

## 网页唤起本地助手（2026-09-08）

- 新增当前用户的 esp-launchpad-enhanced URL 协议入口：macOS AppleScript 应用、Windows HKCU 协议项 + pythonw、Linux desktop + xdg-mime。固定恢复命令，不接收 URL 指定的命令或文件；文件锁阻止并行重复启动。
- 首次授权按钮区分助手离线、恢复中和就绪；离线时用户点击请求系统唤起。另保留已稳定设备的直接授权入口。接管确认立即使旧助手状态失效，防止迟到状态响应覆盖；连接期间不轮询已退出服务。
- 22 项 JavaScript 单元测试、14 项 Python 测试、8 项浏览器回归和 2 项真实 Python 服务/模拟芯片完整流程测试通过。协议跳转在自动化浏览器测试中替换为模拟系统调用，避免测试唤起真实 USB 助手。
- macOS 实测：用户级注册成功，系统 URL 打开后无需终端即恢复新 C6FH4 / 4MB。网页初始授权数 0，用户授权后读取芯片身份并确认接管，后台助手和唤起进程退出。
- 用户确认 Chrome 外部应用打开提示后出现新页面；实际日志显示后续自动连接与 handoff-accepted，助手日志确认退出，4175 端口释放。证明浏览器按钮到系统入口的唤起链路可用。
- 本轮接管后曾尝试实机读取 4KB，出现 Serial data stream stopped；没有据此宣称持续 USB 通信或实机烧录稳定。测试期间部分旧浏览器测试可能触发真实协议入口，已隔离；未单独证实该问题与读取中断的因果关系。
- ZIP、已解压包及当前用户安装入口同步更新。Windows/Linux 系统协议激活未实测，CI 尚未运行。未写入/擦除 Flash 或修改 eFuse，未推送部署。

## 简化连接界面（2026-09-08）

- 主入口统一为“连接设备”，根据已授权端口数量和助手状态分流；恢复中不抢占 USB。备用启动与直接授权移入默认折叠区。
- 8 项浏览器回归通过，涵盖默认折叠、已授权连接、未授权唤起、助手忙碌和就绪、展开后直接授权。未操作实机。
- ZIP、已解压目录和已安装入口同步更新，文件一致。

## 全片擦除、串口控制台与模组重启（2026-09-08）

- 全片擦除调用 esptool-js eraseFlash，需当前下载连接、目标芯片/MAC/容量确认和输入 ERASE。连接变化使旧确认失效；擦除与其他串口操作互斥，失败不自动重试，完成后保持下载模式。
- 控制台使用独立 Web Serial reader，进入监听前释放 loader；支持波特率、更换串口、停止、清空、自动滚动及导出最近 20 万字符。验证 UTF-8 跨块解码、纯文本显示、空闲 reader 取消、拔出/读错误清理及关闭失败后禁止抢占。
- 模组重启采用正常 EN/RTS 脉冲并释放下载引脚状态，按原生 USB/串口桥选择延时；不沿用下载模式复位序列。需确认，可选择重启后监听。界面报告信号发送，USB 重新枚举中断时提示重新监听，不声称已成功启动固件。
- 27 项 JavaScript 单元测试、14 项浏览器回归通过；2 项真实 Python 服务/模拟芯片离线接管测试通过。新增 6 项浏览器场景覆盖擦除确认/失败/失效会话、控制台释放与文本、重启监听和无监听释放。最后的监听错误提示修正另通过 5 项监听单元测试。
- 已检查桌面布局。新功能本轮仅模拟设备验证，未在真实模组执行擦除、重启或读写；尚无这三项功能的实机稳定性结论。
- ZIP、用户已解压包和当前用户安装入口同步更新，未推送部署。

## 统一控制台输出（2026-09-08）

- 控制台显示 esptool 连接/烧录日志及原始串口输出；保留 write/writeLine 的拼接语义，详细诊断默认折叠。
- 6 项操作浏览器回归通过，包括连接日志、换行、工具/模组输出共用控制台以及详细日志折叠。未操作实机。
- 更新 ZIP、已解压包及本机页面副本。

## 紧凑布局与连接状态按钮（2026-09-08）

- 合并连接和断开为同一按钮，连接中可取消，清理失败时可释放连接；串口读写期间保留互斥。
- 压缩标题、卡片和操作间距，擦除/烧录并排，下载帮助及监听说明默认折叠。
- 14 项原有浏览器回归及 1 项新增连接/断开/取消测试通过；检查桌面截图。未操作实机，已同步 ZIP、解压目录与安装入口。

## 停止监听反馈与超时保护（2026-09-08）

- 当前实机开始/停止监听成功；诊断记录多次停止耗时约 5–7 ms 并可重新打开串口，本轮未复现用户报告的持续卡住。没有复位或写入模组。
- 停止增加 3 秒超时、重试停止状态及耗时日志；超时不宣称端口释放，待取消任务完成前禁止新会话。重复停止复用原任务，避免并行关闭。
- 6 项监听单元测试及 1 项控制台浏览器回归通过，包括挂起取消、超时、迟到完成和再次监听。同步本地网页及启动包。
